# Dependencies & build
node_modules/
.next/
out/

# Environment & secrets
.env
.env.local
.env.*.local

# OS & editor
.DS_Store
*.log
Thumbs.db

# Local tooling (bundled Node, not for repo)
.tools/

# Ảnh đã sync (manifest vẫn commit; file .webp tạo bằng npm run sync:media)
public/media/*
!public/media/.gitkeep
